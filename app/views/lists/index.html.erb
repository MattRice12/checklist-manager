<% lists.each do |list| %>
  <div class='list'>
    <p><%= link_to list.name, list %></p>
    <div><%= link_to("Edit List", edit_list_path(list), class: "edit-list") %>
  </div>
<% end %>
<div><%= link_to("New List", new_list_path, class: "new-list") %>
